// Declare all variables
let yemen = "Capital city is Sana'a";

let population=30491000;

let republicOfYemen ={
    language : "Arabic",
}

let republicOfYemen2 = {
   Area  : 555000,
}

// Check data type
console.log(typeof yemen);

console.log(typeof republicOfYemen2);

// Check if data type is the same
if (typeof yemen == 'string') {
    console.log('SAME TYPE');
}
if (typeof republicOfYemen2 == 'object') {
    console.log('SAME TYPE');
}
